<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/search.css" rel="stylesheet">
    <title>Found rooms</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                <div class="card content mt-3">
                    <div class="row">
                        <div>
                            <img src="https://icons.iconarchive.com/icons/goescat/macaron/256/gimp-icon.png" alt="imagen de ususario" class="avatar">
                            <% if (user != undefined){ %>
                                <h3 class="user-name" id="username"><%=user.nick%></h3>
                            <% }else{ %>
                                <h3 class="user-name" id="username"></h3>
                            <% } %>
                        </div>
                    </div>
                    <div class="row mt-2">
                        <div class="col">
                            <h3>Crea tu propia sala</h3>
                            <form action="create-room" method="POST" class="mt-2">
                                <div class="mb-3">
                                  <label class="form-label">Nombre de la sala</label>
                                  <input type="text" class="form-control" name="nombre" required>
                                </div>
                                <div class="mb-3">
                                  <label class="form-label">Contraseña de acceso</label>
                                  <input type="password" class="form-control" name="pass" required>
                                </div>
                                <button type="submit" class="btn btn-primary form-control">Crear</button>
                                <% if(error==1){%>
                                    <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                                        <strong>Lo sentimos</strong> Ha ocurrido un error por favor intente de nuevo.
                                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                    </div>
                                <% } %>
                            </form>
                        </div>
                    </div>
                    <div class="row mt-2">
                        <div class="col">
                            <h3>Ingresa en la sala de un amigo</h3>
                            <form action="join-room" method="POST" class="mt-2">
                                <div class="mb-3">
                                  <label class="form-label">Nombre de la sala</label>
                                  <input type="text" class="form-control" name="nombre" required>
                                </div>
                                <div class="mb-3">
                                  <label class="form-label">Contraseña de acceso</label>
                                  <input type="password" class="form-control" name="pass" required>
                                </div>
                                <button type="submit" class="btn btn-primary form-control">Ingresar</button>
                                <% if(error2==1){%>
                                    <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                                        <strong>Lo sentimos</strong> Ha ocurrido un error por favor intente de nuevo.
                                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                    </div>
                                <% } %>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
    </div>
</body>
</html>
<script>
    if('<%- JSON.stringify(user) %>'!=''){
        var user = '<%- JSON.stringify(user) %>';
        localStorage.setItem('user',user);
    }else{
        var user = JSON.parse(localStorage.getItem('user'));
        $("#username").text(user.nick);
    }
</script>


